services:
  caper:
    build: .
    ports:
      - "5098:5098"
    volumes:
      - ./artefacts:/app/artefacts
      - ./data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DB_PATH=/app/data/tests.db
    restart: unless-stopped
